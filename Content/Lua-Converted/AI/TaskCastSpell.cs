using CoTG.CoTGServer.Scripting.CSharp.Converted;

namespace AIScripts;
//Status: Modified script, not like original
public class TaskCastSpellAI : CAIScript
{
    /*
    void UpdatePriority(object c)
    {
        float b = 0.3f;
        int d = GetMP();
        int e = GetTarget();

        if (e != 0) // Check if a target exists
        {
            int maxMP = GetMaxMP();
            float ratio = (float)d / maxMP;
            float priority = ratio * 0.8f * 2 * b;
            c.Priority = priority;
            return true;
        }
        else
        {
            c.Priority = 0;
        }
    }

    void BeginTask(object c)
    {
        Random random = new Random();
        int d = 0;
        int e = 3;
        int randomValue = random.Next(d, e); // Generate a number between 0 and 3

        if (CanUseSpell(randomValue) == 0)
        {
            return;
        }
    }
    */
}

